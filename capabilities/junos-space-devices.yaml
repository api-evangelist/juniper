naftiko: 1.0.0-alpha2
info:
  label: Juniper Networks Junos Space Network Management Platform API — Devices
  description: 'Juniper Networks Junos Space Network Management Platform API — Devices. 4 operations. Lead operation: Juniper
    Networks List managed devices. Self-contained Naftiko capability covering one Juniper business surface.'
  tags:
  - Juniper
  - Devices
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    JUNIPER_API_KEY: JUNIPER_API_KEY
capability:
  consumes:
  - type: http
    namespace: junos-space-devices
    baseUri: https://{space_server}/api/space
    description: Juniper Networks Junos Space Network Management Platform API — Devices business capability. Self-contained,
      no shared references.
    resources:
    - name: device-management-devices
      path: /device-management/devices
      operations:
      - name: listdevices
        method: GET
        description: Juniper Networks List managed devices
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: filter
          in: query
          type: string
          description: Filter expression for devices
      - name: discoverdevice
        method: POST
        description: Juniper Networks Discover a device
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: device-management-devices-device_id
      path: /device-management/devices/{device_id}
      operations:
      - name: getdevice
        method: GET
        description: Juniper Networks Get device details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: deletedevice
        method: DELETE
        description: Juniper Networks Delete managed device
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: basic
      username: '{{env.JUNIPER_USER}}'
      password: '{{env.JUNIPER_PASS}}'
  exposes:
  - type: rest
    namespace: junos-space-devices-rest
    port: 8080
    description: REST adapter for Juniper Networks Junos Space Network Management Platform API — Devices. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/device-management/devices
      name: device-management-devices
      description: REST surface for device-management-devices.
      operations:
      - method: GET
        name: listdevices
        description: Juniper Networks List managed devices
        call: junos-space-devices.listdevices
        with:
          filter: rest.filter
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: discoverdevice
        description: Juniper Networks Discover a device
        call: junos-space-devices.discoverdevice
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/device-management/devices/{device-id}
      name: device-management-devices-device-id
      description: REST surface for device-management-devices-device_id.
      operations:
      - method: GET
        name: getdevice
        description: Juniper Networks Get device details
        call: junos-space-devices.getdevice
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletedevice
        description: Juniper Networks Delete managed device
        call: junos-space-devices.deletedevice
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: junos-space-devices-mcp
    port: 9090
    transport: http
    description: MCP adapter for Juniper Networks Junos Space Network Management Platform API — Devices. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: juniper-networks-list-managed-devices
      description: Juniper Networks List managed devices
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: junos-space-devices.listdevices
      with:
        filter: tools.filter
      outputParameters:
      - type: object
        mapping: $.
    - name: juniper-networks-discover-device
      description: Juniper Networks Discover a device
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: junos-space-devices.discoverdevice
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: juniper-networks-get-device-details
      description: Juniper Networks Get device details
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: junos-space-devices.getdevice
      outputParameters:
      - type: object
        mapping: $.
    - name: juniper-networks-delete-managed-device
      description: Juniper Networks Delete managed device
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: junos-space-devices.deletedevice
      outputParameters:
      - type: object
        mapping: $.
